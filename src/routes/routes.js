const routes = {
  HEALTH_CHECK: '/health-check',

  // AUTH
  LOGIN: '/auth/login',

  // USER
  USERS: '/user',
  USER: '/user/:id',
  ME: '/me',
  // POST
  POSTS: '/post',
  POST: '/post/:id'
}

module.exports = routes
